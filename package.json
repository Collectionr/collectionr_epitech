{
  "name": "collectionr",
  "version": "0.0.1",
  "private": true,
  "description": "Root Git tooling for the monorepo (Husky hooks) — see backend/ for the application.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10"
  }
}
